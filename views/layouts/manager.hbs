<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modern Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f4f7fe; /* Màu nền sáng hiện đại hơn */
        }

        /* --- Sidebar Customization --- */
        .sidebar {
            position: fixed;
            top: 0; bottom: 0; left: 0;
            z-index: 100;
            padding: 70px 0 0;
            box-shadow: inset -1px 0 0 rgba(0, 0, 0, .1);
            background-color: #1a1c23; /* Màu tối đậm hơn */
            transition: all 0.3s;
        }
        .sidebar-sticky {
            position: relative;
            top: 0;
            height: calc(100vh - 48px);
            padding-top: .5rem;
            overflow-x: hidden;
            overflow-y: auto;
        }
        .sidebar .nav-link {
            font-weight: 400;
            color: #a0aec0;
            padding: 0.8rem 1.5rem;
            border-left: 4px solid transparent;
            transition: all 0.2s ease;
        }
        .sidebar .nav-link:hover {
            color: #fff;
            background-color: rgba(255,255,255,.05);
        }
        .sidebar .nav-link.active {
            color: #fff;
            background-color: rgba(255,255,255,.05);
            border-left-color: #4e73df; /* Highlight màu xanh */
        }
        .sidebar-heading {
            font-size: .7rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #718096;
            padding-left: 1.5rem;
        }

        /* --- Navbar Customization --- */
        .navbar-custom {
            background-color: #fff;
            box-shadow: 0 2px 4px rgba(0,0,0,.08);
            z-index: 101;
        }
        .avatar-img {
            width: 40px; height: 40px; object-fit: cover; border-radius: 50%;
        }

        /* --- Main Content Enhancement --- */
        main { padding-top: 75px; }
        @media (min-width: 768px) {
            .content-wrapper { margin-left: 250px; }
            .sidebar { width: 250px; }
        }

        /* --- Cards & Gradients --- */
        .card-modern {
            border: none;
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.05);
            transition: transform 0.2s;
        }
        .card-modern:hover { transform: translateY(-3px); }

        /* Gradients cho các thẻ thống kê */
        .bg-gradient-primary { background: linear-gradient(45deg, #4e73df, #224abe); color: white;}
        .bg-gradient-success { background: linear-gradient(45deg, #1cc88a, #13855c); color: white;}
        .bg-gradient-info { background: linear-gradient(45deg, #36b9cc, #258391); color: white;}
        .bg-gradient-warning { background: linear-gradient(45deg, #f6c23e, #dda20a); color: white;}

        .stat-icon-wrapper {
            background: rgba(255,255,255,0.2);
            padding: 15px;
            border-radius: 50%;
        }

        /* --- Table Improvements --- */
        .table-modern thead th {
            border-top: none;
            border-bottom: 2px solid #e3e6f0;
            font-weight: 600;
            text-transform: uppercase;
            font-size: 0.8rem;
            color: #8898aa;
        }
        .status-badge {
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
        }
        .status-active { background-color: #d1f2eb; color: #0f5132; }
        .status-inactive { background-color: #fadbd8; color: #842029; }

    </style>
</head>
<body>

<header class="navbar navbar-expand navbar-light fixed-top navbar-custom px-4">
    <a class="navbar-brand fw-bold text-primary" href="#">
        <i class="bi bi-grid-fill me-2"></i>PetCareUS
    </a>
    <button class="navbar-toggler d-md-none collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#sidebarMenu">
        <span class="navbar-toggler-icon"></span>
    </button>
    
    <ul class="navbar-nav ms-auto align-items-center">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center pe-0" href="#" role="button" data-bs-toggle="dropdown">
                
                <div class="d-flex flex-column align-items-end">
                    <div class="fw-bold">{{authUser.full_name}}</div>
                    <small class="text-muted">Manager</small>
                </div>
                <i class="bi bi-person-circle fs-2 ms-2"></i>
            </a>
            <ul class="dropdown-menu dropdown-menu-end shadow border-0">
                <li><a class="dropdown-item" href="/profile"><i class="bi bi-person me-2"></i> Hồ sơ</a></li>
                <li><a class="dropdown-item" href="#"><i class="bi bi-gear me-2"></i> Cài đặt</a></li>
                <li><hr class="dropdown-divider"></li>
                <li>
                        <form id="frmSignOut" action="/signout" method="post" style="display: none;"></form>
                        <a class="dropdown-item py-2 text-danger fw-bold"
                            href="javascript: document.getElementById('frmSignOut').submit();">
                            <i class="bi bi-box-arrow-right me-2"></i>Đăng xuất
                        </a>
                </li>
            </ul>
        </li>
    </ul>
</header>

<div class="container-fluid">
    <div class="row">
        <nav id="sidebarMenu" class="col-md-3 col-lg-2 d-md-block sidebar collapse">
            <div class="sidebar-sticky pt-3">
                <h6 class="sidebar-heading mt-3 mb-2">Quản lý chính</h6>
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link" href="/manager" data-route="/manager">
                            <i class="bi bi-speedometer2 me-3"></i> Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/manager/customers" data-route="/manager/customers">
                            <i class="bi bi-people-fill me-3"></i> Khách hàng
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/manager/staffs" data-route="/manager/staffs">
                            <i class="bi bi-people me-3"></i> Nhân viên
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/manager/appointments" data-route="/manager/appointments">
                            <i class="bi bi-receipt me-3"></i> Lịch hẹn
                        </a>
                    </li>
                </ul>

                <h6 class="sidebar-heading mt-4 mb-2">Báo cáo & Phân tích</h6>
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link" href="/manager/revenue" data-route="/manager/revenue">
                            <i class="bi bi-bar-chart-line me-3"></i> Doanh thu
                        </a>
                    </li>
                </ul>
            </div>
        </nav>

        <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4 pb-4 content-wrapper">
            
            {{{body}}}

        </main>

    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

<script>
    // Active menu based on current URL
    document.addEventListener('DOMContentLoaded', function() {
        const currentPath = window.location.pathname;
        const navLinks = document.querySelectorAll('.nav-link[data-route]');
        
        // Remove all active classes first
        navLinks.forEach(link => {
            link.classList.remove('active');
        });
        
        // Find matching route and add active class
        let matchedLink = null;
        
        // Exact match first
        navLinks.forEach(link => {
            const route = link.getAttribute('data-route');
            if (route === currentPath) {
                matchedLink = link;
            }
        });
        
        // If no exact match, try partial match (for routes with parameters)
        if (!matchedLink) {
            navLinks.forEach(link => {
                const route = link.getAttribute('data-route');
                if (currentPath.startsWith(route) && route !== '/manager') {
                    matchedLink = link;
                }
            });
        }
        
        // Default to dashboard if no match or if on manager root
        if (!matchedLink || currentPath === '/manager' || currentPath === '/manager/') {
            matchedLink = document.querySelector('.nav-link[data-route="/manager"]');
        }
        
        // Add active class to matched link
        if (matchedLink) {
            matchedLink.classList.add('active');
        }
    });
</script>

{{{_section.js}}}
</body>
</html>