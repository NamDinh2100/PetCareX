<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PetCareX - Hệ thống quản lý thú y</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        body {
            background-color: #f8f9fa;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        .card {
            border-radius: 0.5rem;
        }

        .navbar-brand {
            font-weight: bold;
        }
    </style>

    {{{_section.js}}}
</head>

<body>

    {{#if isAuth}}
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm mb-4">
        <div class="container">
            <a class="navbar-brand" href="/"><i class="fas fa-paw me-2"></i>PetCareX</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        {{!-- SỬA TẠI ĐÂY: authUser.full_name --}}
                        <span class="nav-link text-white">Xin chào, {{authUser.full_name}}</span>
                    </li>
                    <li class="nav-item">
                        <form id="frmSignOut" action="/signout" method="post" style="display: none;"></form>
                        <a class="dropdown-item py-2 text-danger fw-bold"
                            href="javascript: document.getElementById('frmSignOut').submit();">
                            <i class="bi bi-box-arrow-right me-2"></i>Đăng xuất
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    {{/if}}

    {{{body}}}

    {{{_section.js}}}
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>