<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Receptionist - PetCare</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f4f7fe;
        }

        .sidebar {
            position: fixed;
            top: 0;
            bottom: 0;
            left: 0;
            z-index: 100;
            padding: 70px 0 0;
            box-shadow: inset -1px 0 0 rgba(0, 0, 0, .1);
            background-color: #1a1c23;
            transition: all 0.3s;
        }

        .sidebar-sticky {
            position: relative;
            top: 0;
            height: calc(100vh - 48px);
            padding-top: .5rem;
            overflow-x: hidden;
            overflow-y: auto;
        }

        .sidebar .nav-link {
            font-weight: 400;
            color: #a0aec0;
            padding: 0.8rem 1.5rem;
            border-left: 4px solid transparent;
            transition: all 0.2s ease;
        }

        .sidebar .nav-link:hover {
            color: #fff;
            background-color: rgba(255, 255, 255, .05);
        }

        .sidebar .nav-link.active {
            color: #fff;
            background-color: rgba(255, 255, 255, .05);
            border-left-color: #4e73df;
        }

        .sidebar-heading {
            font-size: .7rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            color: #718096;
            padding-left: 1.5rem;
        }

        .navbar-custom {
            background-color: #fff;
            box-shadow: 0 2px 4px rgba(0, 0, 0, .08);
            z-index: 101;
        }

        .avatar-img {
            width: 40px;
            height: 40px;
            object-fit: cover;
            border-radius: 50%;
        }

        main {
            padding-top: 75px;
        }

        @media (min-width: 768px) {
            .content-wrapper {
                margin-left: 250px;
            }

            .sidebar {
                width: 250px;
            }
        }

        .card-modern {
            border: none;
            border-radius: 15px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
            transition: all 0.3s;
        }

        .card-modern:hover {
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.1);
        }

        .table-modern {
            font-size: 0.95rem;
        }

        .table-modern thead {
            background-color: #f8f9fc;
            color: #6c757d;
            font-weight: 600;
            font-size: 0.85rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .table-modern tbody tr {
            transition: all 0.2s;
        }

        .table-modern tbody tr:hover {
            background-color: #f8f9fc;
        }

        .status-badge {
            padding: 0.35rem 0.65rem;
            border-radius: 20px;
            font-weight: 500;
            font-size: 0.75rem;
        }

        .status-active {
            background-color: #d4edda;
            color: #155724;
        }

        .status-inactive {
            background-color: #f8d7da;
            color: #721c24;
        }

        .breadcrumb {
            background-color: transparent;
            padding: 0;
            margin-bottom: 0.5rem;
            font-size: 0.875rem;
        }

        .breadcrumb-item+.breadcrumb-item::before {
            content: "›";
            font-size: 1.2rem;
        }

        .breadcrumb-item a {
            color: #6c757d;
            text-decoration: none;
        }

        .text-xs {
            font-size: 0.7rem;
        }

        .text-gray-300 {
            color: #d1d3e2 !important;
        }

        .text-gray-800 {
            color: #5a5c69 !important;
        }
    </style>

    {{{_section.css}}}
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-custom fixed-top px-3">
        <a class="navbar-brand fw-bold text-primary" href="#">
            <i class="bi bi-grid-fill me-2"></i>PetCareUS
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-center">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button"
                        data-bs-toggle="dropdown">
                        <div class="d-flex flex-column align-items-end">
                            <div class="fw-bold">{{authUser.full_name}}</div>
                            <small class="text-muted">Receptionist</small>
                        </div>
                        <i class="bi bi-person-circle fs-2 ms-2"></i>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end shadow-sm">
                        <li><a class="dropdown-item" href="#"><i class="bi bi-person me-2"></i>Hồ sơ</a></li>
                        <li><a class="dropdown-item" href="#"><i class="bi bi-gear me-2"></i>Cài đặt</a></li>
                        <li>
                            <hr class="dropdown-divider">
                        </li>
                        <li>
                            <form id="frmSignOut" action="/signout" method="post" style="display: none;"></form>
                            <a class="dropdown-item py-2 text-danger fw-bold"
                                href="javascript: document.getElementById('frmSignOut').submit();">
                                <i class="bi bi-box-arrow-right me-2"></i>Đăng xuất
                            </a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container-fluid">
        <div class="row">
            <nav class="col-md-2 d-md-block sidebar">
                <div class="sidebar-sticky">
                    <h6
                        class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-3 mb-3 text-muted">
                        <span class="text-white">Quản lý</span>
                    </h6>
                    <ul class="nav flex-column">
                        <li class="nav-item">
                            <a class="nav-link" href="/receptionist" data-route="/receptionist">
                                <i class="bi bi-speedometer2 me-3"></i>
                                Tổng quan
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/receptionist/appointments"
                                data-route="/receptionist/appointments">
                                <i class="bi bi-calendar-check me-3"></i>
                                Lịch hẹn
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/receptionist/invoice" data-route="/receptionist/invoice">
                                <i class="bi bi-receipt-cutoff me-3"></i>
                                Hóa đơn
                            </a>
                        </li>
                    </ul>
                </div>
            </nav>

            <main class="col-md-10 ms-sm-auto content-wrapper px-md-4">
                <div class="container-fluid">
                    {{{body}}}
                </div>
            </main>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Auto-detect active menu based on current URL
        document.addEventListener('DOMContentLoaded', function () {
            const currentPath = window.location.pathname;
            const menuLinks = document.querySelectorAll('.sidebar .nav-link[data-route]');

            // Remove all active classes first
            menuLinks.forEach(link => {
                link.classList.remove('active');
            });

            // Find matching route
            let matchedLink = null;

            // Exact match first
            menuLinks.forEach(link => {
                const route = link.getAttribute('data-route');
                if (route === currentPath) {
                    matchedLink = link;
                }
            });

            // If no exact match, try partial match (for routes with parameters)
            if (!matchedLink) {
                menuLinks.forEach(link => {
                    const route = link.getAttribute('data-route');
                    if (currentPath.startsWith(route + '/') && route !== '/receptionist') {
                        matchedLink = link;
                    }
                });
            }

            // Default to dashboard if no match or if on receptionist root
            if (!matchedLink || currentPath === '/receptionist' || currentPath === '/receptionist/') {
                matchedLink = document.querySelector('.nav-link[data-route="/receptionist"]');
            }

            // Add active class to matched link
            if (matchedLink) {
                matchedLink.classList.add('active');
            }
        });
    </script>
    {{{_section.js}}}
</body>

</html>