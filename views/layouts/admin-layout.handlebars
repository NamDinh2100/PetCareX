<!DOCTYPE html>
<html lang="vi">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PetCareUS</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Abhaya+Libre:wght@400;500;600;700;800&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" />

    <link rel="stylesheet" href="/layout.css">


    {{{_sections.css}}}

</head>

<body>
    {{!-- ================= 1. HEADER DÙNG CHUNG ================= --}}
    <nav class="navbar navbar-expand-lg bg-white border-bottom sticky-top py-3 px-4 shadow-sm">
        <div class="container-fluid">
            <a class="navbar-brand d-flex align-items-center fw-bold brand-text" href="/admin/dashboard">
                <img src="/static/images/logo.png" alt="Logo" width="40" height="40" class="me-2">
                PetCareUS
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="mainNav">
                <ul class="navbar-nav mx-auto mb-2 mb-lg-0">

                </ul>

                <div class="d-flex align-items-center">
                    {{!-- User Profile Dropdown --}}
                    <div class="dropdown">
                        <a href="#"
                            class="d-flex align-items-center text-decoration-none text-dark dropdown-toggle hidden-arrow"
                            data-bs-toggle="dropdown">

                            <div class="text-end me-2 lh-1 d-none d-md-block">
                                <div class="fw-bold small text-uppercase">{{authUser.role}}</div>
                                <div class="text-secondary" style="font-size: 0.8rem;">{{authUser.full_name}}</div>
                            </div>

                            <div id="navAvatar" class="shadow-sm"
                                style="width: 40px; height: 40px; background-color: #ff0000; color: #fff; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: bold; font-size: 1.2rem; font-family: 'Abhaya Libre', serif; border: 2px solid #000000;">
                                        </div>

                        </a>

                        <ul class="dropdown-menu dropdown-menu-end shadow border-0 mt-2 rounded-3">
                            <li><a class="dropdown-item py-2" href="/admin/profile"><i class="bi bi-person me-2"></i>My
                                    Profile</a></li>
                            <li><a class="dropdown-item py-2" href="#" data-bs-toggle="modal" data-bs-target="#changePasswordModal"><i class="bi bi-key me-2"></i>Change password</a>
                            </li>
                            <li>
                                <hr class="dropdown-divider">
                            </li>
                            <li>
                                <form id="frmSignOut" action="/signout" method="post" style="display: none;"></form>
                                <a class="dropdown-item py-2 text-danger fw-bold"
                                    href="javascript: document.getElementById('frmSignOut').submit();">
                                    <i class="bi bi-box-arrow-right me-2"></i>Logout
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <div class="container-fluid flex-grow-1">
        <div class="row h-100 w-100">
            <nav id="sidebarMenu" class="col-md-2 col-lg-2 d-md-block bg-white sidebar collapse border-end p-3 h-100">
                <div class="list-group list-group-flush mt-2">
                    <a href="/admin/customers"
                        class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i
                            class="bi bi-person-fill me-3"></i> USER</a>
                    <a href="/admin/employees"
                        class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i
                            class="bi bi-people-fill me-3"></i> EMPLOYEE</a>
                    <a href="/admin/medicines"
                        class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i
                            class="bi bi-capsule me-3"></i> MEDICINE</a>
                    <a href="/admin/appointments"
                        class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i
                            class="bi bi-calendar3 me-3"></i> APPOINTMENT</a>
                    <a href="/admin/services"
                        class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i
                            class="bi bi-tools me-3"></i> SERVICE</a>
                    <a href="/admin/statistical"
                        class="list-group-item list-group-item-action border-0 rounded-3 mb-1 py-3 fw-bold text-secondary"><i
                            class="bi bi-graph-up me-3"></i> STATISTICAL</a>
                </div>
            </nav>

            <main id="mainContent" class="col-md-10 col-lg-10 ms-sm-auto px-md-4 py-4 bg-light">
                {{{body}}}
            </main>
        </div>
    </div>

    <!-- Change Password Modal -->
    <div class="modal fade" id="changePasswordModal" tabindex="-1" aria-labelledby="changePasswordModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content rounded-4 border-2">
                <div class="modal-header bg-danger text-white rounded-top-4">
                    <h5 class="modal-title fw-bold" id="changePasswordModalLabel" style="font-family: 'Abhaya Libre', serif;">
                        <i class="bi bi-key me-2"></i>Change Password
                    </h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body p-4">
                    <form id="changePasswordForm" method="POST" action="/admin/change-password">
                        <div class="mb-3">
                            <label for="currentPassword" class="form-label fw-bold" style="font-family: 'Abhaya Libre', serif;">Current Password</label>
                            <input type="password" class="form-control rounded-pill border-2 border-dark" id="currentPassword" name="current_password" required>
                        </div>
                        <div class="mb-3">
                            <label for="newPassword" class="form-label fw-bold" style="font-family: 'Abhaya Libre', serif;">New Password</label>
                            <input type="password" class="form-control rounded-pill border-2 border-dark" id="newPassword" name="new_password" required>
                        </div>
                        <div class="mb-3">
                            <label for="confirmPassword" class="form-label fw-bold" style="font-family: 'Abhaya Libre', serif;">Confirm New Password</label>
                            <input type="password" class="form-control rounded-pill border-2 border-dark" id="confirmPassword" name="confirm_password" required>
                        </div>
                        <div id="passwordError" class="alert alert-danger d-none" role="alert">
                            <i class="bi bi-exclamation-triangle me-2"></i>
                            <span id="errorMessage"></span>
                        </div>
                        <div id="passwordSuccess" class="alert alert-success d-none" role="alert">
                            <i class="bi bi-check-circle me-2"></i>
                            Password changed successfully!
                        </div>
                    </form>
                </div>
                <div class="modal-footer border-0 px-4 pb-4">
                    <button type="submit" form="changePasswordForm" class="btn btn-outline-danger rounded-pill px-4">Change Password</button>
                </div>
            </div>
        </div>
    </div>

   


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            // --- 1. XỬ LÝ AVATAR CHỮ CÁI ĐẦU ---
            const navFullName = "{{authUser.full_name}}";
            const navAvatarEl = document.getElementById('navAvatar');
            
            if (navAvatarEl) {
                let initial = "A"; // Mặc định là A nếu không có tên
                if (navFullName && navFullName.trim().length > 0) {
                    // Lấy ký tự đầu và viết hoa
                    initial = navFullName.trim().charAt(0).toUpperCase();
                }
                navAvatarEl.innerText = initial;
            }

            // --- 2. XỬ LÝ SIDEBAR & NAVBAR (Code cũ của bạn) ---
            const sidebar = document.getElementById('sidebarMenu');
            const mainContent = document.querySelector('main');
            const currentPath = window.location.pathname;

            // Ẩn Sidebar nếu đang ở trang Profile
            if (currentPath.includes('/admin/profile')) {
                if (sidebar) {
                    sidebar.classList.remove('d-md-block', 'd-block');
                    sidebar.classList.add('d-none');
                    sidebar.style.display = 'none';
                }
                if (mainContent) {
                    mainContent.classList.remove('col-md-10', 'col-lg-10', 'ms-sm-auto');
                    mainContent.classList.add('col-12');
                }
            }

            // Active state sidebar
            if (sidebar) {
                sidebar.querySelectorAll('.list-group-item').forEach(link => {
                    if (currentPath.includes(link.getAttribute('href'))) {
                        link.classList.add('active');
                        link.classList.remove('text-secondary');
                    }
                });
            }

            // Active state navbar
            const mainNav = document.getElementById('mainNav');
            if (mainNav) {
                mainNav.querySelectorAll('.nav-link').forEach(link => {
                    if (link.getAttribute('href') === currentPath) {
                        link.classList.add('active');
                    }
                });
            }

            // --- 3. XỬ LÝ CHANGE PASSWORD MODAL ---
            const changePasswordForm = document.getElementById('changePasswordForm');
            if (changePasswordForm) {
                changePasswordForm.addEventListener('submit', async function(e) {
                    e.preventDefault();

                    
                    const currentPassword = document.getElementById('currentPassword').value;
                    const newPassword = document.getElementById('newPassword').value;
                    const confirmPassword = document.getElementById('confirmPassword').value;
                    
                    const errorDiv = document.getElementById('passwordError');
                    const successDiv = document.getElementById('passwordSuccess');
                    const errorMessage = document.getElementById('errorMessage');
                    
                    // Reset alerts
                    errorDiv.classList.add('d-none');
                    successDiv.classList.add('d-none');
                    
                    // Validation
                    if (newPassword !== confirmPassword) {
                        errorMessage.textContent = 'New password and confirmation do not match.';
                        errorDiv.classList.remove('d-none');
                        return;
                    }
                    
                    if (newPassword.length < 6) {
                        errorMessage.textContent = 'New password must be at least 6 characters long.';
                        errorDiv.classList.remove('d-none');
                        return;
                    }
                    
                    try {
                        const response = await fetch('/admin/change-password', {
                            method: 'POST',
                            headers: {
                                'Content-Type': 'application/json',
                            },
                            body: JSON.stringify({
                                currentPassword: currentPassword,
                                newPassword: newPassword
                            })
                        });
                        
                        const result = await response.json();
                        
                        if (response.ok) {
                            successDiv.classList.remove('d-none');
                            changePasswordForm.reset();
                            setTimeout(() => {
                                const modal = bootstrap.Modal.getInstance(document.getElementById('changePasswordModal'));
                                modal.hide();
                                successDiv.classList.add('d-none');
                            }, 2000);
                        } else {
                            errorMessage.textContent = result.message || 'Failed to change password.';
                            errorDiv.classList.remove('d-none');
                        }
                    } catch (error) {
                        errorMessage.textContent = 'An error occurred. Please try again.';
                        errorDiv.classList.remove('d-none');
                    }
                });
            }
        });

    </script>
    {{{_sections.js}}}
</body>

</html>